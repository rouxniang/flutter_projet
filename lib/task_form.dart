import 'package:flutter/material.dart';

class TaskFormPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Ajouter une tâche")),
      body: Center(child: Text("Formulaire ici")),
    );
  }
}
